# Basketball Scoreboard - Tkinter 버전
# Tkinter는 Python 기본 내장이므로 추가 설치 불필요

# 필수 의존성 (Supabase Realtime 기능용)
supabase>=2.0.0
python-dotenv>=1.0.0

# 선택적 의존성 (필요시에만 설치)
# pygame>=2.0.0  # 백업 버전용 (scoreboard_pygame_backup.py)
# numpy>=1.21.0  # 백업 버전용